name: Nightly

on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu
    steps:
      - name: Get commit ID
        id: get_commit_id
        run: echo "COMMIT_ID=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
      - name: Save commit ID to file
        run: echo "${{ steps.get_commit_id.outputs.COMMIT_ID }}" > commit_id.txt
